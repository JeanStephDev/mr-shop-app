<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Réseau : appels API, WebView de paiement GeniusPay -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Géolocalisation : centrer la carte sur l'adresse de livraison -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Notifications push (Android 13+ exige la permission explicite) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="MR Shop"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Android App Links : si l'app est installée, cliquer sur un
                 lien https://store.hexa-node.site/... l'ouvre directement
                 dans l'app plutôt que dans le navigateur. Nécessite de
                 publier /.well-known/assetlinks.json sur ce domaine — voir
                 BUILD_AND_SIGN_APK.md, section "App Links Android". Sans ce
                 fichier, autoVerify échoue silencieusement et les liens
                 s'ouvrent simplement dans le navigateur (comportement de
                 repli sûr, rien ne casse). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="store.hexa-node.site"/>
            </intent-filter>

            <!-- Android App Links : si l'app est installée, un lien
                 https://store.hexa-node.site/... l'ouvre directement au lieu
                 du navigateur (ex: partage d'un produit). Nécessite de
                 publier un fichier assetlinks.json signé sur
                 https://store.hexa-node.site/.well-known/assetlinks.json —
                 voir BUILD_AND_SIGN_APK.md, section "App Links Android". -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="store.hexa-node.site"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- Google Maps : remplace par ta vraie clé une fois obtenue sur
             console.cloud.google.com (voir BUILD_AND_SIGN_APK.md). Sans clé
             réelle, l'app compile et se lance normalement — seul l'écran de
             sélection d'adresse sur carte affichera une erreur Google Maps. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_MAPS_API_KEY_HERE"/>

        <!-- AdMob : ID de test officiel Google par défaut, remplace-le par ton
             vrai App ID une fois ton compte AdMob créé (voir README_FLUTTER.md).
             Avec cet ID de test, l'app fonctionne et affiche des pubs factices. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

    </application>

    <!-- Permet à Flutter/plugins de vérifier si des apps de traitement de texte sont dispo -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Nécessaire pour url_launcher (appel téléphonique au livreur, liens externes) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

</manifest>
